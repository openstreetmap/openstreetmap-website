<% if @changesets.newer_items_cursor %>
  <div class="changeset_more my-3">
    <div class="text-center position-absolute start-0 end-0" hidden>
      <div class="spinner-border" role="status">
        <span class="visually-hidden"><%= t("browse.start_rjs.loading") %></span>
      </div>
    </div>
    <ul class="pagination justify-content-center">
      <li class="page-item">
        <%= link_to t(".newer_changesets"), url_for(:after => @changesets.newer_items_cursor), :class => "page-link" %>
      </li>
    </ul>
  </div>
<% end %>
<% if @changesets.items.present? %>
  <ol class="list-group list-group-flush">
    <%= render @changesets.items %>
  </ol>
<% elsif params[:bbox] %>
  <p class="mx-3"><%= params[:before] ? t(".no_more_area") : t(".empty_area") %></p>
<% elsif params[:display_name] %>
  <p class="mx-3"><%= params[:before] ? t(".no_more_user") : t(".empty_user") %></p>
<% else %>
  <p class="mx-3"><%= params[:before] ? t(".no_more") : t(".empty") %></p>
<% end %>
<% if @changesets.older_items_cursor -%>
  <div class="changeset_more my-3">
    <div class="text-center position-absolute start-0 end-0" hidden>
      <div class="spinner-border" role="status">
        <span class="visually-hidden"><%= t("browse.start_rjs.loading") %></span>
      </div>
    </div>
    <ul class="pagination justify-content-center">
      <li class="page-item">
        <%= link_to t(".older_changesets"), url_for(:before => @changesets.older_items_cursor), :class => "page-link" %>
      </li>
    </ul>
  </div>
<% end -%>
